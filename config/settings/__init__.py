from .base import *

# Make settings a Python package

# Empty file to complete package structure

"""
Django settings package initialization.
"""

import os
print(f"Loading settings package from {__file__}")
print(f"Package directory contents: {os.listdir(os.path.dirname(__file__))}")
