import { LitElement, html } from 'lit';
import { customElement } from 'lit/decorators.js';

@customElement('settings-view')
export class SettingsView extends LitElement {
  render() {
    return html`<div>Settings (coming soon)</div>`;
  }
}
